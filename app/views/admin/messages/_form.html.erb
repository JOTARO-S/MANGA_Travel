<div class="row p-3">
    <h3>チャット</h3>
    <div class="col-md-12">
      <%= form_with(model: [chat, chat_message], local:true) do |form| %>
      <%= form.text_area :message_content, rows: '5', class: "form-control" %>
      <%= form.submit "送信", class: "mt-1" %>
      <% end %>
    </div>
  </div>
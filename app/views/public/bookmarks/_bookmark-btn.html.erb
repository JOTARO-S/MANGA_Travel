    <% if post.bookmarked_by?(current_user) %>
        
        <%= link_to "保存済み", post_bookmarks_path(post.id),method: :delete, class: "btn btn-secondary", remote: true %>

    <% else %>
    
        <%= link_to "ブックマークに保存", post_bookmarks_path(post.id),method: :post, class: "btn btn-secondary", remote: true %>
        
    <% end %>
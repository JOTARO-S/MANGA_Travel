<% if current_user.following?(@user) %>
    <%= link_to "フォロー中", user_relationships_path(@user.id), class: "btn btn-outline-success btn-block", method: :delete, remote: true %>
<% else %>
    <%= link_to "フォロー", user_relationships_path(@user.id), class: "btn btn-success btn-block", method: :post, remote: true %>
<% end %>
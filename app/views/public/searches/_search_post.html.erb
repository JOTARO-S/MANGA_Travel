<h2>投稿検索 for "<%= @search_params %>"</h2>

<% if @posts.empty? %>
  <h4>検索結果はありません。</h4>
<% else %>

<div class="container">
    <div class="row mt-3">
        <div class="col-md-12">
            <table class='table table-hover table-inverse mt-3'>
              <thead>
                <tr>
                  <th>タイトル</th>
                  <th>住所・施設名</th>
                  <th>所在地</th>
                  <th>ユーザー名</th>
                  <th></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% @posts.each do |post| %>
                 <% if post.is_active == false %>
                 <% if post.draft_status == false %>
                  <tr>
                    <td><%= post.name.truncate(15) %></td>
                    <td><%= post.place.truncate(15) %></td>
                    <td><%= post.get_category_name.truncate(15) %></td>
                    <td><%= link_to post.user.name.truncate(15), user_path(post.user.id) %></td>
                    <td><%= link_to "詳細", post_path(post), class: "btn btn-success" %></td>
                    <% if user_signed_in? %>
                    <td><div class="bookmark_btn_<%= post.id %> col-md-2">
                     <%= render "public/bookmarks/bookmark-btn", post: post %></div>
                    </td>
                    <% end %>
                  </tr>
                 <% end %>
                 <% end %>  
                <% end %>
              </tbody>
            </table>
        </div>
    </div>
</div>

<% end %>
  
  